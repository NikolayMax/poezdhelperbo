version: '3.8'

services: 
  app:
    build: ./
    environment:
      - REDIS_URL=redis://bot-redis:6379
    depends_on:
      - redis
  redis:
    image: redis
    container_name: bot-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
